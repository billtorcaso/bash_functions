
###--------------------------------------------

#   This is fundamental to all other "_BT_*" env variables.

export _BT_GIT_REPOS_HERE="$HOME/billtorcaso-repos-here"

###--------------------------------------------

#   Things related to my bash_functions repo

export _BT_BASH_REPO="$_BT_GIT_REPOS_HERE/bash_functions"
export _BT_BASH_FNED_FILES="$_BT_BASH_REPO/generic.bash_functions $_BT_BASH_REPO/bt.bash_functions"
function fnsrepo()  { cd $_BT_BASH_REPO/$1; }
function fndef()    { declare -f "$@"; }
function fns()      {
    for thing in $(echo $_BT_BASH_FNED_FILES)
    do
        [[ -r $thing ]] && source $thing
    done
}
function fned()     { vi $_BT_BASH_FNED_FILES "$@" && fns ; }

###--------------------------------------------

#   Things related to my billtorcaso.org repo

export _BT_BILLTORCASO_ORG_REPO="$_BT_GIT_REPOS_HERE/billtorcaso_org"
function btorepo()  { cd $_BT_BILLTORCASO_ORG_REPO/billtorcaso_org/$1; }
function bto()      { btorepo billtorcaso_org/$1; }

###--------------------------------------------

#   Activate the definitions in other files 

fns;

true
